node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# 로컬 환경변수 (API 키) — 절대 커밋하지 않는다
.env
.env.local
.env*.local

# 로컬 데이터베이스 (작성한 글·순위 이력) — 개인 데이터라 커밋하지 않는다
data/db.json

.DS_Store
*.log

# 상위노출 조사가 받아온 원본 HTML — 다시 받으면 되므로 커밋하지 않는다
study/.cache/

# 한 파일로 묶은 스킬 (npm run skills:bundle) — 원본 docs/skills/*/ 에서 언제든 다시 만든다
docs/skills/bundled/
