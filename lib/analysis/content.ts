/**
 * 글 내용의 균형 — 「읽는 사람이 가져갈 게 있나」 vs 「홍보만 하고 있나」.
 *
 * **왜 만들었나.** 회원이 물었다 — "시설 안내·이벤트 안내 같은 홍보성 글로만 하면 잘 안
 * 띄어주는 것 같다. 시설+이벤트 +알파를 써야 되는 것 같은데?" 그래서 상위 글 본문을
 * 실제로 세봤다 (2026-08-05, 「쌍용동 헬스장」·「봉명동 헬스장」 상위 11편):
 *
 *                  1~3위 평균   4위 이하 평균
 *   정보 요소        5.2개        3.6개
 *   홍보 요소        2.0개        3.8개
 *   경험 요소        3.7개        4.8개
 *
 *   순위 상관 (11편): 정보 +0.59 · 홍보 -0.37 · 경험 -0.13
 *
 * 가설의 앞부분은 맞았다 — 홍보 표현이 많은 글이 아래에 있었다. 그런데 「+알파」의 정체는
 * **후기 형식이 아니라 운동 정보**였다. 경험 요소는 순위와 관계가 없었고(-0.13), 반례가
 * 분명했다 — 「신방동헬스장 고민 끝! … 피앤피짐 소개」는 경험 요소 0개인 순수 소개글인데
 * 2위였다. 그 글은 정보 요소 4개(자세·식단·유산소·스트레칭)에 홍보 요소는 2개뿐이었다.
 *
 * 즉 갈린 지점은 「후기냐 홍보냐」가 아니라 **「읽는 사람이 가져갈 게 있느냐」** 였다.
 *
 * **표본이 작다.** 11편·키워드 2개이고, 두 키워드에서 결과가 갈렸다 (봉명동 정보 +0.79 /
 * 쌍용동 +0.15). 그래서 이 값을 「법칙」으로 쓰지 않는다 — 관찰소(factors.ts)가 매일 다시
 * 재고, 방향이 뒤집히면 화면에서 그게 보인다.
 *
 * ─────────────────────────────────────────────────────────────────────────
 *
 * **재측정 (2026-08-06, 키워드 22개 × 상위 10편 · 본문 확보 161편).**
 *
 * 회원이 물었다 — "이벤트 문구는 줄이지 않는다. 홍보만 있는 글이 위인지, 정보가 조금
 * 섞인 글이 위인지 데이터로 보여달라." 그래서 **홍보 표현이 하나라도 있는 141편**만
 * 남기고(순수 정보글 20편 제외) 정보 종류별로 1~3위 비율을 셌다:
 *
 *                        편수    평균순위   1~3위    95% 구간
 *   정보 0종류(순수 홍보)     2편    6.50위      0%    0~66%
 *   정보 1~2종류            17편    5.88위     18%    6~41%
 *   정보 3~4종류            23편    6.57위     17%    7~37%     ← 3~4종류는 이득이 없다
 *   정보 5~7종류            45편    4.87위     40%   27~55%
 *   정보 8종류 이상          54편    4.85위     43%   30~56%
 *
 * 답: **정보를 섞은 쪽이 위다.** 다만 계단은 3종류가 아니라 **5종류**에 있었다 —
 * 3~4종류(17%)는 1~2종류(18%)와 구분되지 않고, 5종류를 넘기면 40%대로 뛴다.
 * 그래서 홍보글 정보 하한을 4 → 5 로 올렸다 (4·5·6 중 어디든 방향은 같다 — INFO_MIN 주석).
 *
 * 그리고 **홍보를 줄이는 것은 근거가 없었다** (회원의 판단이 맞았다):
 *
 *                     홍보 1~3종류        홍보 4종류 이상
 *   정보 0~2종류        13편 1~3위  8%     6편 1~3위 33%
 *   정보 3~4종류        15편 1~3위 27%     8편 1~3위  0%
 *   정보 5종류 이상      68편 1~3위 43%    31편 1~3위 39%
 *
 * 정보가 충분하면 홍보 4종류 이상도 39% 다 (43% 와 구간이 겹친다). 정보가 모자란 쪽에서
 * 홍보를 줄여도 올라가지 않았다 (2종류 이하 17% / 3종류 초과 16%). 즉 **홍보 종류 수는
 * 순위를 가르지 않고, 정보 종류 수가 가른다.**
 *
 * 1~3위 홍보글이 실제로 쓴 홍보 낱말: 상담 68% · 이벤트 34% · 영업시간 27% · 예약 27% ·
 * 문의 24% · 주차 가능 20% · 혜택 20%. 이벤트·혜택은 상위권이 **쓰는** 말이다.
 *
 * **재현했다** (같은 키워드 22개를 study 도구로 다시 수집, 홍보 섞인 글 135편):
 *
 *                 1차(141편)   2차(135편)
 *   정보 0~2종류      16%         18%
 *   정보 3~4종류      17%         21%
 *   정보 5~7종류      40%         40%      ← 정보 5종류 계단은 두 번 다 나왔다
 *   정보 8종류 이상    43%         37%
 *
 *   홍보 1~2종류      36%         37%
 *   홍보 4종류        19%         14%
 *   홍보 5종류        47%         46%
 *   홍보 7종류 이상    14%(7편)    44%(9편)  ← 뒤집혔다. 7편으로는 아무 말도 못 한다
 *
 * 그래서 결론을 나눴다. **정보 하한 5 는 실측이다** (두 번 재현). **홍보 상한 6 은 실측이
 * 아니라 판단이다** — 순위로는 어떤 상한도 근거가 없었고, 위 표의 7종류 이상 칸이 그걸
 * 보여준다. 6 을 남겨둔 이유는 「홍보 낱말을 거의 다 쓴 글」만 걸러내려는 것이고,
 * 그래서 검수 가중치도 3 → 2 로 내렸다 (lib/writing/checker.ts 의 promo-restraint).
 *
 * 재현: `npm run study:collect` → `npm run study:analyze` (study/README.md)
 */

/**
 * 정보 요소 — 읽는 사람이 가져갈 수 있는 실행 지식.
 *
 * 「어떤 기구가 있다」가 아니라 「그 기구로 무엇을 어떻게 한다」에 나오는 말들이다.
 */
export const INFO_WORDS = [
  '자세', '루틴', '식단', '칼로리', '단백질', '유산소', '근력', '근육', '스트레칭',
  '세트', '횟수', '호흡', '자극', '부위', '방법', '순서', '주의', '초보', '코어',
  '하체', '상체', '가슴', '등운동', '어깨', '스쿼트', '데드리프트', '벤치프레스',
  '워밍업', '회복', '수분', '체지방', '인바디', '무게', '반복', '유의', '팁',
]

/**
 * 홍보 요소 — 파는 말.
 *
 * 하나도 없으면 안 된다 (우리 글은 상담으로 이어져야 한다). 처음엔 많으면 내려간다고
 * 봤지만, 141편 재측정에서 그게 아니었다 — 정보가 5종류 이상이면 홍보 4종류 이상도
 * 1~3위 39% 다. 상위권이 가장 많이 쓴 홍보 낱말이 「상담」(68%)·「이벤트」(34%) 다.
 */
export const PROMO_WORDS = [
  '문의', '상담', '예약', '오픈', '이벤트', '할인', '프로모션', '특가', '혜택', '신규',
  '회원권', '영업시간', '오시는 길', '주차 가능', '전화', '카톡', '마감', '선착순',
  '지금 바로', '서둘러', '무료 체험', '등록 문의',
]

/** 경험 요소 — 겪은 사람만 쓸 수 있는 말 (순위와는 관계가 없었지만 신뢰도에는 쓰인다) */
export const EXPERIENCE_WORDS = [
  '다녀왔', '다니고 있', '등록했', '등록하고', '수업', '받아봤', '받았는데', '느꼈',
  '좋았', '아쉬', '솔직', '후기', '체험', '처음 갔', '가봤', '주차', '개월', '주째',
  '일차', '일째', '변화', '줄었', '빠졌', '늘었',
]

/**
 * 정보 요소는 이만큼은 있어야 한다.
 *
 * 계단이 5종류에 있었고, 다시 재도 같았다 (3~4종류 1~3위 17%/21% · 5~7종류 40%/40%).
 * 다만 **누적 비교로는 4·5·6 이 다 비슷하다** (5 이상 38% / 4 이상 36% / 6 이상 40%,
 * 구간이 겹친다). 그래서 5 는 「구간 안에서 계단이 있던 자리」이고, 4 나 6 이 틀렸다는
 * 뜻은 아니다. 방향(정보가 많은 글이 위)만 두 번 다 뚜렷했다 (ρ -0.21 / -0.17).
 */
export const INFO_MIN = 5

/**
 * 정보 하한은 **글의 목적에 따라 다르다.**
 *
 * 회원이 물었다 — "후기 블로그에 정보를 욱여넣으면 이상하지 않을까?" 맞는 말이고,
 * 실측도 같은 쪽을 가리킨다. 경험 요소는 순위와 관계가 없었고(-0.13) 오히려 4위 이하가
 * 더 많았다 (경험 4.8 vs 1~3위 3.7). 결정적인 반례는 2위였던 「신방동헬스장 고민 끝!
 * … 피앤피짐 소개」 — **경험 요소 0개인 순수 소개글**이 정보 4개·홍보 2개로 2위였다.
 *
 * 그래서 후기글에 정보를 밀어넣어 순위를 노리는 것은 두 번 틀린다. 후기의 유일한 무기인
 * 「제3자가 실제로 겪은 말투」를 잃고, 그 대가로 얻는 순위 근거도 없다.
 *
 * 역할을 나눈다.
 *   info   5 — 정보가 주인공이다.
 *   promo  5 — **4 에서 올렸다.** 141편 재측정에서 3~4종류(1~3위 17%)는 1~2종류(18%)와
 *              구분되지 않았고, 5종류를 넘겨야 40%대가 됐다.
 *
 *              **2026-08-21 — 하한은 그대로, 채우는 자리만 바뀌었다.** 회원 요청으로
 *              홍보글에서 「운동 정보」 구간을 뺐다. 하한이 깨지는지 먼저 쟀다 (짐작으로
 *              실측값을 낮출 수는 없다): 운동 정보 없이 시설·신뢰·이벤트만으로 홍보글을
 *              써서 countSignals 로 세니 **정보 7종류**였다 — 자세·루틴·유산소·세트·초보·
 *              스쿼트·무게. 시설을 구체적으로 적으면 유산소존·프리웨이트가 나오고,
 *              신뢰 구간에서 「자세가 무너지면 무게를 먼저 내립니다」를 쓰면 나머지가 찬다.
 *
 *              즉 **운동을 가르쳐야 채워지는 값이 아니었다.** 가르치는 것과 우리가 무엇을
 *              하는지 말하는 것은 다르고, 이 값이 요구한 것은 뒤쪽이다.
 *   review 2 — 순위는 다른 글이 뚫는다. 후기는 상담 직전에 읽는 신뢰용이다.
 *              그래도 0 은 아니다 — 「가서 뭘 알게 됐는지」가 하나도 없는 후기는
 *              읽는 사람이 가져갈 게 없다. 다만 **들은 말을 옮기는 정도**로 족하다.
 */
export const INFO_MIN_BY_TYPE: Record<'promo' | 'info' | 'review', number> = {
  promo: 5,
  info: 5,
  review: 2,
}

/**
 * 정보가 이만큼 있으면 홍보 표현을 더 세도 순위가 내려가지 않았다.
 * (정보 5종류 이상 · 홍보 4종류 이상 = 1~3위 39% / 홍보 1~3종류 = 43% · 구간이 겹친다)
 */
export const INFO_RELEASE = 5

/**
 * 홍보 요소는 이만큼까지.
 *
 * **3 에서 6 으로 올렸다.** 두 번 재는 동안 홍보 종류 수는 순위를 가르지 않았다 —
 * 1~2종류 36/37% · 4종류 19/14% · 5종류 47/46% 로 오르내리기만 하고 95% 구간이 겹친다.
 *
 * **이 값은 실측이 아니라 판단이다.** 순위로는 어떤 상한도 근거가 없었다 (7종류 이상 칸이
 * 14% → 44% 로 뒤집혔다, 각각 7편·9편). 6 을 남겨둔 것은 「홍보 낱말을 거의 다 쓴 글」
 * 하나만 걸러내려는 것이고, 그래서 이 항목의 검수 가중치는 2 다 (가장 낮은 쪽).
 */
export const PROMO_MAX = 6

/**
 * 홍보 상한은 **목적에 따라 다르다.**
 *
 * 셋 다 순위 근거가 아니다 — 위 PROMO_MAX 주석에 적었듯 홍보 종류 수는 순위를 가르지
 * 않았다. 그래서 세 값의 근거를 목적으로 적어 둔다:
 *   promo  6 — 「파는 말을 거의 다 쓴 글」만 걸러낸다. 이벤트 문구를 줄이라는 뜻이 아니다.
 *   info   4 — 마지막 한 구간에 홍보를 모으면 이 정도가 된다 (아래 「2 → 4」 참고).
 *   review 3 — 방문자가 선착순·영업시간·프로모션을 나열하면 대가성 광고로 읽힌다.
 *              저품질 위험 쪽 규칙이다.
 *
 * ─── info 2 → 4 (2026-08-10) ────────────────────────────────
 *
 * 회원 요청으로 정보글의 마지막 구간을 「문의 한 줄」에서 「정보 8 : 홍보 2」로 늘렸다:
 * "화자는 센타 입장에서 정보성 주제를 알려주는 느낌으로 해주고 정보성 8 : 홍보성 2 느낌으로
 * 글 마지막에는 홍보가 들어갈 수 있게 해줘."
 *
 * 그 구간을 제대로 쓰면 상담·예약·전화·문의만으로 4종류가 된다. 상한을 2로 두면 지시문은
 * 「홍보를 넣어라」, 검수는 「홍보를 줄여라」가 되어 서로 싸운다. **완화의 방향이 순위를
 * 걸지도 않는다** — 홍보 종류 수는 어느 경계에서도 순위를 가르지 않았다 (위 PROMO_MAX 주석).
 *
 * 대신 조이는 곳을 옮겼다: 홍보를 **어디에** 쓰는지를 본다 (checker 의 `info-promo-tail`).
 * 종류 수를 줄이라고 하는 것보다, 앞 구간에 안 섞였는지 보는 것이 회원이 말한 것에 가깝다.
 */
export const PROMO_MAX_BY_TYPE: Record<'promo' | 'info' | 'review', number> = {
  promo: 6,
  info: 4,
  /*
   * 후기 3 → 4 (2026-08-11).
   *
   * 회원이 후기글 **제목에도 홍보성**을 넣어달라고 했다. 홍보 낱말은 제목과 본문을 합쳐서
   * 세므로(checker.ts 의 scanText), 제목에 「혜택」 한 조각이 들어가면 본문 예산이 그만큼
   * 줄어든다. 그러면 지시문(제목에 혜택을 넣어라)과 검수기(홍보가 많다)가 서로 싸운다.
   *
   * 그래서 **제목 몫으로 한 칸만** 올린다. 본문에서 쓸 수 있는 종류는 전과 같이 3 이다.
   * 이 상한은 순위 기준이 아니라 대가성 광고로 읽히지 않게 하는 장치라(아래 주석) 그
   * 취지를 유지한 채 자리만 옮긴 것이다.
   */
  review: 4,
}

/**
 * 상담 유도 낱말 — 「우리한테 와서 얘기해보세요」에 해당하는 말.
 *
 * **방향은 확실하고 정확한 선은 아니다** (2026-08-07 측정 → 08-10 정정).
 * 다른 홍보 낱말과 달리 이건 순위와 방향이 일관됐다 — 세 표본에서 ρ -0.23 ~ -0.35,
 * 시험한 모든 경계에서 상담을 더 쓴 쪽이 위였다. 다만 아래 「정정」 항목을 함께 읽어야 한다.
 *
 * 비방문자 글 84편 · 「상담·예약·문의」 등장 **횟수**(종류가 아니다):
 *
 *   0~1회   29편   평균 6.45위   1~3위 14%  ( 5~31%)
 *   1~3회   32편        4.75위         34%  (20~52%)
 *   3~6회   13편        4.92위         31%  (13~58%)
 *   6회 이상 10편        3.30위         60%  (31~83%)
 *
 *   밀도 1,000자당 3.0 이상 8편 → 평균 3.13위 · 1~3위 75% (41~93%)
 *   밀도 0.5 미만        31편 → 평균 6.32위 · 1~3위 16% ( 7~33%)   ← 구간이 갈린다
 *
 * 재현 (같은 조사 도구, 다른 런 / 화자 안 가림):
 *   런2 전체 155편 ρ=-0.26 · 런1 전체 157편 ρ=-0.23 · 비방문자 84편 ρ=-0.35
 *   세 경우 모두 「밀도 3+ vs 0.5-」 구간이 갈렸다.
 *
 * ─── 정정 (2026-08-10, 런 3회로 다시 재고) ─────────────────────────────────
 *
 * 위 「구간이 갈렸다」는 **하루치 순위**로 잰 것이다. 조사 도구에 이 항목을 넣고
 * 순위를 세 런의 **중간값**으로 안정시켜 다시 재니 구간이 겹쳤다 (비방문자 88편):
 *
 *   1회 이상 59편 1~3위 39% (28~52%)  |  0회  29편 17% ( 8~35%)   ← 22%p, 겹침
 *   6회 이상 11편        45% (21~72%)  |  나머지 77편 30% (21~41%)  ← 16%p, 겹침
 *   상관 ρ=-0.24
 *
 * 즉 **「상담을 권하라」는 방향은 믿을 만하고, 「6회」라는 정확한 선은 표본 오차 안이다.**
 * 가장 큰 계단은 0회 → 1회 사이다 (17% → 39%). 6 을 목표로 남겨둔 이유는 방향이 일관되고
 * 6회 이상 집단이 실제로 가장 좋았기 때문이지, 5회가 위험하다는 뜻이 아니다.
 *
 * 조사 도구의 판정은 `keep` 이다 — 바꿀 근거가 없다는 뜻이고, 그래서 6 을 유지한다.
 *
 * **글자수 때문이 아니다** — 글자수 자체의 상관은 -0.11~-0.18 로 더 약하고, 밀도 3+ 집단은
 * 오히려 더 길었다 (중간 2,014자 vs 1,679자).
 *
 * ─── 그래서 무엇이 바뀌나 ───────────────────────────────────────────────
 *
 * 처음에 이 앱은 「홍보 표현을 줄여라」였다. 두 번 뒤집혔다.
 *   ① 홍보 표현 **종류 수**는 순위와 무관했다 (PROMO_MAX 주석).
 *   ② 상담 유도 낱말 **횟수**는 순위를 갈랐다 — **많이 쓴 쪽이 위다.**
 *
 * 둘은 모순이 아니다. 종류를 늘리는 것(할인·특가·선착순·마감…)과 「상담·예약·문의」를
 * 여러 번 말하는 것은 다른 일이다. 전자는 전단지가 되고 후자는 「오시라」는 말이다.
 * 그래서 검수도 두 항목으로 나눠 둔다 — 종류는 6종류 이하, 상담 유도는 6회 이상.
 *
 * 회원이 말한 목적과도 같은 방향이다 — "홍보성 글의 목적은 상담 예약을 하게 만드는 것".
 */
export const CTA_WORDS = ['상담', '예약', '문의']

/** 홍보글에서 상담 유도 낱말은 이만큼은 나와야 한다 (6회 이상 1~3위 53~60% / 0~1회 14~26%) */
export const CTA_MIN_BY_TYPE: Record<'promo' | 'info' | 'review', number> = {
  promo: 6,
  /*
   * 정보글·후기글은 낮게 둔다. 실측은 「비방문자 글」 전체에서 나왔지만, 정보글의 목적은
   * 신뢰 축적이고 후기글의 화자는 방문객이다 — 방문객이 「상담」을 여섯 번 말하면 대가성
   * 광고로 읽힌다. 순위를 조금 잃더라도 그쪽을 지킨다.
   *
   * **1 → 2** (2026-08-10). 정보글 마지막 구간이 「문의 한 줄」에서 「센터 소개 + 상담 유도
   * 350~450자」가 됐다 (회원 요청, 정보 8 : 홍보 2). 그 구간을 쓰면서 상담을 한 번도 안
   * 권하면 구간이 비는 것이라 하한을 2로 올린다. 6(홍보글)과는 여전히 멀다 — 정보글에서
   * 상담을 여섯 번 말하면 정보글이 아니다.
   */
  info: 2,
  review: 2,
}

/** 상담 유도 낱말이 몇 번 나왔나 (종류가 아니라 **횟수**를 센다 — 실측이 횟수였다) */
export function countCta(text: string): { count: number; found: Record<string, number> } {
  const hay = text ?? ''
  const found: Record<string, number> = {}
  let count = 0
  for (const w of CTA_WORDS) {
    const n = hay.split(w).length - 1
    if (n > 0) found[w] = n
    count += n
  }
  return { count, found }
}

export interface ContentSignals {
  /** 몇 종류가 들어 있는지 (같은 말을 여러 번 써도 1로 센다) */
  info: number
  promo: number
  experience: number
  /** 실제로 들어 있던 말 — 화면에서 근거로 보여준다 */
  infoFound: string[]
  promoFound: string[]
  experienceFound: string[]
}

/**
 * 글에 든 신호를 센다 (순수 함수 — 테스트 대상).
 *
 * **횟수가 아니라 종류를 센다.** 「자세」를 열 번 써도 1개다. 같은 말을 반복하는 것은
 * 정보를 더한 게 아니고, 횟수로 세면 키워드 반복하듯 채워 넣어 점수를 올릴 수 있다.
 */
export function countSignals(text: string): ContentSignals {
  const hay = text ?? ''
  const pick = (words: string[]) => words.filter((w) => hay.includes(w))
  const infoFound = pick(INFO_WORDS)
  const promoFound = pick(PROMO_WORDS)
  const experienceFound = pick(EXPERIENCE_WORDS)
  return {
    info: infoFound.length,
    promo: promoFound.length,
    experience: experienceFound.length,
    infoFound,
    promoFound,
    experienceFound,
  }
}

export type BalanceLevel = 'good' | 'thin' | 'pushy' | 'both'

export interface ContentBalance {
  level: BalanceLevel
  signals: ContentSignals
  /** 정보 쪽 한 줄 */
  infoNote: string
  /** 홍보 쪽 한 줄 */
  promoNote: string
}

/**
 * 균형 판정 (순수 함수 — 테스트 대상).
 *
 * 판정은 두 축을 따로 낸다 — 정보가 모자란 것과 홍보가 과한 것은 고칠 방법이 다르다.
 * 정보가 모자라면 단락을 **더해야** 하고, 홍보가 과하면 **덜어내야** 한다.
 */
export function contentBalance(
  text: string,
  /** 글 유형 — 정보 하한이 목적에 따라 다르다 (INFO_MIN_BY_TYPE 주석) */
  type: 'promo' | 'info' | 'review' = 'promo'
): ContentBalance {
  const signals = countSignals(text)
  const infoMin = INFO_MIN_BY_TYPE[type] ?? INFO_MIN
  const promoMax = PROMO_MAX_BY_TYPE[type] ?? PROMO_MAX
  const thin = signals.info < infoMin
  const pushy = signals.promo > promoMax

  const level: BalanceLevel = thin && pushy ? 'both' : thin ? 'thin' : pushy ? 'pushy' : 'good'

  /*
   * 후기글에는 다른 말을 한다.
   * 「단락을 하나 더하세요」는 정보글·홍보글에 맞는 처방이고, 후기에 그렇게 하면
   * 업체가 쓴 글이 된다. 후기는 들은 말을 옮기는 정도로 족하다.
   */
  const infoNote = thin
    ? type === 'review'
      ? `가서 알게 된 것이 ${signals.info}종류입니다 (${infoMin}종류면 충분). 정보 단락을 만들지 마세요 — 상담 때 들은 말을 한 줄로 옮기면 됩니다. 예) "제 자세를 보면서 어디에 자극이 와야 하는지 짚어주셨어요."`
      : `읽는 사람이 가져갈 정보가 ${signals.info}종류입니다 (${infoMin}종류 이상). 실측에서 이 항목이 순위를 가장 뚜렷하게 갈랐습니다 — 홍보 섞인 글 141편 중 정보 3~4종류는 1~3위 17%인데 5종류 이상은 40~43%였습니다. 자세·호흡·세트·무게·초보 중 하나로 단락을 하나 더하세요 — 「기구가 있다」가 아니라 「그 기구로 무엇을 어떻게 한다」로 씁니다.`
    : `정보가 ${signals.info}종류 들어 있습니다 (${signals.infoFound.slice(0, 5).join('·')}${signals.infoFound.length > 5 ? ' 등' : ''}) — ${type === 'review' ? '후기로는 충분합니다.' : '상위권 수준입니다 (1~3위 홍보글 중간값 7종류).'}`

  /*
   * 홍보가 많다고 「덜어내라」고 하면 안 된다.
   *
   * 141편 재측정에서 홍보 종류 수는 순위와 관계가 없었다 (정보 5종류 이상이면 홍보
   * 4종류 이상도 39%, 정보가 모자란 쪽에서는 홍보를 줄여도 16~17% 그대로). 예전 문구는
   * 「이벤트·상담 안내를 한 곳에 모으세요」였는데, 그건 회원이 팔아야 하는 문장을
   * 근거 없이 지우게 하는 조언이었다. 지금은 두 갈래로 말한다:
   *   - 정보가 모자란 상태(thin)면 → 홍보를 덜지 말고 정보를 더하라
   *   - 상한을 넘겼으면 → 파는 말을 거의 다 쓴 상태다. 같은 일을 하는 수단만 합쳐라
   *     (이때도 「혜택 내용을 줄여라」가 아니다 — 상한 자체가 순위 근거가 아니다)
   */
  const promoNote = pushy
    ? `홍보 표현이 ${signals.promo}종류입니다 (${signals.promoFound.slice(0, 8).join('·')}) — 이 유형 상한은 ${promoMax}종류입니다.${
        type === 'promo'
          ? ` **이 상한은 순위 기준이 아닙니다.** 실측에서 홍보 종류 수는 순위를 가르지 않았습니다 (4종류 1~3위 14~19% / 5종류 46~47%). 파는 말을 거의 다 쓴 글만 걸러내는 선입니다 — 혜택 내용은 그대로 두고, 겹치는 수단(문의·상담·전화처럼 같은 일을 하는 말)만 한 곳에 합치세요.`
          : ` 이 유형에서는 순위가 아니라 글의 목적 때문에 조여 둡니다 — ${type === 'review' ? '방문자가 선착순·영업시간을 나열하면 대가성 광고로 읽힙니다.' : '정보글이 쌓으려는 신뢰를 스스로 깎습니다.'}`
      }`
    : thin && signals.promo >= 3
      ? `홍보 표현이 ${signals.promo}종류입니다 — 상한(${promoMax}종류) 안입니다. **이건 줄이지 마세요.** 실측에서 홍보 종류를 줄여도 순위는 오르지 않았습니다 (정보가 모자란 글: 홍보 2종류 이하 17% / 3종류 초과 16%). 위의 정보 단락을 더하는 것이 유일하게 효과가 있던 쪽입니다.`
      : `홍보 표현이 ${signals.promo}종류입니다 — 이 글 유형의 범위(${promoMax}종류 이하) 안입니다.${
          signals.promo === 0 ? ' 다만 하나도 없으면 상담으로 이어지지 않습니다 — 마지막에 한 번은 넣으세요.' : ''
        }`

  return { level, signals, infoNote, promoNote }
}
